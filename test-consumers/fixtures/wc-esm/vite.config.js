import { defineConfig } from 'vite';

// Vanilla Vite app (no framework plugin) that registers the viewer custom
// element through the packaged element entry via a bundler ESM import.
export default defineConfig({});
