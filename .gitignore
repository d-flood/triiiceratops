# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/
jspm_packages/

# Build outputs
dist/
dist-ssr/
build/
*.local

# Generated code (see SPEC "Dependency, Type, And Generated-Code Policy"):
# emitted under ignored, clearly named generated dirs; type-checked via build.
# icons.ts (ticket 02) + the paraglide message runtime (compiled from
# ./messages into ./src/paraglide, then staged into ./src/lib/paraglide by
# build:lib). Ignored here via config rather than relying on paraglide's own
# self-generated .gitignore.
packages/core/src/lib/generated/
packages/core/src/lib/paraglide/
packages/core/src/paraglide/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# OS files
.DS_Store
Thumbs.db

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn
.yarn-integrity
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# parcel-bundler cache
.cache
.parcel-cache

# TypeScript cache
*.tsbuildinfo

# Svelte
.svelte-kit/

# Temporary files
*.tmp
*.temp
temp/
tmp/

# Test coverage
coverage/
*.lcov
.nyc_output
playwright-report/
test-results/

# Performance comparison output (raw JSON + traces, uploaded as CI artifacts)
perf-results/

# Release artifacts (ticket 27): local `pnpm release:pack --out release-artifacts`
# output — six .tgz + SHA256SUMS + release-manifest.json, uploaded as CI artifacts.
release-artifacts/

# Storybook
storybook-static/


# docs
.venv/
__pycache__
site/
# Generated effective zensical config (docs-build.mjs); never committed.
.zensical.effective.toml
docs/demo*
docs/viewer*
docs/svelte*