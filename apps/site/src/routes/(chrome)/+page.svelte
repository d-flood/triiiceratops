<script lang="ts">
    import PageHead from '$lib/PageHead.svelte';

    /**
     * The front page. Its whole body is the hero — the running viewer beside the
     * configuration that composes it — which `PageHead` renders in place of the
     * ordinary heading block for this path.
     */
</script>

<PageHead />
