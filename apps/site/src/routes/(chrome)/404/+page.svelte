<script lang="ts">
    /**
     * The not-found page the static host serves for every unmatched path.
     *
     * Prerendered here and relocated to `/404.html` by
     * `scripts/finish-build.mjs`: SvelteKit prerenders a page route to a
     * directory index, and the host looks for a bare `404.html` at the root.
     */
</script>

<div class="pagehead">
    <h1>That page is not here</h1>
    <p class="lede">
        The link may be old or mistyped. The navigation lists the site's main
        pages.
    </p>
</div>
