---
'triiiceratops': patch
---

Update the tool panels to stack vertically instead of horizontally. Update PDF Export plugin to take a custom filename. This also includes a significant internal refactor that did not touch the public API.
