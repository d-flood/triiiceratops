---
'triiiceratops': minor
---

Add a gallery-placement picker to the toolbar: a flyout menu, modeled on the viewing-mode menu, offering the four dock sides (`Top`, `Bottom`, `Left`, `Right`). It replaces the deleted drag-to-dock gesture, and like the toolbar's other flyout menus it is operable by pointer, touch, and keyboard. The button shares the gallery toggle's `toolbar.showGallery` gate.
